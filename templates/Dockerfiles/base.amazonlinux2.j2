FROM {{ base_image }}

# Update basic system packages
RUN yum clean all && \
    yum update -y && \
    yum install -y git && \
    amazon-linux-extras install -y ansible2 && \
    yum clean all && \
    rm -rf /var/cache/yum

COPY docker_image_history.* /etc
